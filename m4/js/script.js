// the equality (==) operator uses type conversion to decide if these things are equal. Reference --> http://www.c-point.com/javascript_tutorial/jsgrpComparison.htm

// alert(0==false); // returns true

// the identity (===) operator behaves the same way except that it does not do any type conversion. So the two things being compared must be the same type in order to be equal.

alert (0===false); //returns false
